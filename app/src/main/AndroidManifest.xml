<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.test_proiect">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Test_Proiect">
        <activity android:name=".QuizActivity"></activity>
        <activity android:name=".MainQuizActivity" />
        <activity android:name=".ZicaleActivity" />
        <activity android:name=".TraditiiActivity" />
        <activity android:name=".LegendeUrbaneActivity" />
        <activity android:name=".LegendeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ProfileActivity" />
    </application>

</manifest>